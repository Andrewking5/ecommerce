import { FullPageLoader } from '@/src/components/guitar';
import { ESPRESSO_DARK } from '../constants';

export default function AdminLoader() {
  return <FullPageLoader bgStyle={ESPRESSO_DARK} />;
}
